## 1. 地图坐标协议与 NX 真源

- [x] 1.1 在 NX 主控 OpenSpec 0026 中补充已保存地图 origin yaw 的唯一格式、旧数据处理和验证要求
- [x] 1.2 在主控真源 `MapInfo` 末尾追加 `origin_yaw_rad`，让地图保存器、仓库和主控完整写入、校验并返回 origin 三元组
- [x] 1.3 为拒绝缺失 yaw 的旧地图包、非有限四元数、非零 yaw、字段编号和主控响应增加 NX 单元测试
- [x] 1.4 同步 Android 主控协议副本并扩展跨端字段编号契约测试

## 2. 地图预览与坐标纯逻辑

- [x] 2.1 实现有界 `MapPreviewAssembler`，覆盖分片乱序、重复冲突、版本变化、长度、SHA-256、超时和尺寸上限测试
- [x] 2.2 实现 App 私有目录的原子预览缓存和总字节淘汰策略，覆盖缓存命中、损坏与哈希变化测试
- [x] 2.3 实现包含 letterbox 的屏幕、图片、栅格和世界坐标双向变换，覆盖非零 origin yaw、纵轴翻转、边界与往返误差
- [x] 2.4 实现地图导航状态 reducer，覆盖会话、地图 revision、定位状态、控制权和目标代次变化时的草稿与路径失效

## 3. 主控客户端地图与导航协议

- [x] 3.1 增加地图列表与单选地图预览请求、响应匹配、超时、合并和回调
- [x] 3.2 增加切图、停止运行时和设置初始位姿命令，并继续携带权威状态 revision
- [x] 3.3 增加目标规划、路径事件匹配、开始导航和取消接口，拒绝过期 task、地图与目标代次
- [x] 3.4 扩展真实 ROUTER mock server 集成测试，覆盖地图预览、切图定位、初始位姿、路径乱序、导航、取消和重连不续传目标
- [x] 3.5 同步 `NavigationPath` 协议并接入客户端回调，按会话、任务、地图和路径序号拒绝过期实时路线

## 4. 控制器状态与 Compose 工作区

- [x] 4.1 将地图列表、选中地图、预览缓存、定位与导航 reducer 接入进程级 Controller，保持两条 ZMQ 连接和零速度语义不变
- [x] 4.2 实现地图管理界面，显示版本、尺寸、创建时间、当前标记、预览状态、刷新、切图和停止运行时操作
- [x] 4.3 实现定位导航工作区，显示地图、权威机器人位姿、定位/健康/控制权/任务状态和明确阻断原因
- [x] 4.4 实现按下定位、拖动定向的初始位姿与目标编辑模式，发送前显示数值与地图版本确认
- [x] 4.5 实现规划路径、长度、开始导航和取消界面，目标修改、断连或定位丢失时立即撤销旧确认
- [x] 4.6 保留建图工作区、视频、摇杆、主屏地图全屏切换和维护模式下的 RViz 工程链路
- [x] 4.7 将地图调整为横屏主视觉，并让初始位姿和目标编辑自动进入全屏、实时显示拖动位置与角度
- [x] 4.8 区分初始位姿与导航目标的栅格门禁，允许初始位姿落在机器人占地或激光盲区形成的未知栅格，导航目标继续拒绝未知与占用栅格
- [x] 4.9 从按下事件保存位姿位置，消除触摸拖动阈值造成的选点平移误差
- [x] 4.10 在主控提交 `current_map` 前使用活动 `LOAD_MAP` 任务的地图 identity 选择和发送初始位姿
- [x] 4.11 允许待机状态重新加载主控记录的上次当前地图，并覆盖停止后重启同一地图的回归测试
- [x] 4.12 定位成功或运行时停止后清除旧初始位姿草稿与交互提示，防止下次加载同图时误复用
- [x] 4.13 在导航地图上独立绘制实时全局路线，显示长度、点数和序号，并在全部导航终态、切图、定位停止和会话变化时清空
- [x] 4.14 将导航页规整为地图主视觉、关键状态摘要和按任务阶段变化的动作栏，实现地图管理、状态详情及加载地图、发送初始位姿、停止定位、开始导航确认对话框；地图支持有界双指缩放、平移和一键还原且缩放后选点准确；主页合并任务入口并保留全部六种姿态动作直接显示
- [x] 4.15 建立固定深色语义主题和统一排版，关闭系统浅色跟随与动态取色，统一原生窗口、页面、卡片、输入框、按钮以及全部产品对话框的表面、文字、圆角、描边和按钮布局
- [x] 4.16 按现场交互反馈撤销姿态动作折叠，移除更多动作状态和对话框，恢复底部六种姿态动作的单击直达
- [x] 4.17 让导航状态详情在固定高度内纵向滚动完整显示权威状态、路线统计和全部阻断原因，关闭操作保持常驻
- [x] 4.18 用统一当前步骤解析驱动动作栏和状态详情：待机选图直接显示加载地图，定位未启动使用中性状态，只展示系统真实阻断及当前步骤门禁并将稳定原因码转换为中文
- [x] 4.19 补全地图加载与等待初始位姿的中性定位摘要，并在重连后地图列表自动选中权威当前地图时自动从缓存或主控恢复预览
- [x] 4.20 将缩放后的地图图像、路线和位姿叠加层裁剪在定位导航地图组件内，防止非全屏浏览时绘制越界

## 5. 验证与真机分阶段验收

- [x] 5.1 严格校验两个仓库的 OpenSpec 与语言规则，增量构建 NX 受影响包并串行运行相关测试
- [x] 5.2 通过 Android 核心单元测试、debug 构建和 G20 工程 Mock 横屏截图检查
- [ ] 5.3 保持 MANUAL、零速度和无活动目标，在真机验证图传链路地图列表、预览、切图、初始位姿、定位跟踪和规划预览
- [ ] 5.4 模拟主控断连、地图 revision 变化和定位丢失，验证旧目标与路径不恢复且取消导航始终可用
- [ ] 5.5 仅在用户明确确认安全场地和人员监护后，真机验证开始导航、取消、取消后再次规划与到达；此前保持运动验收未完成
- [x] 5.6 记录部署顺序、回滚步骤以及代码级、Mock、无运动真机和真实运动四类验收边界
- [x] 5.7 覆盖实时路线连续更新、乱序、旧任务、地图冲突、清空和新会话补发的 reducer 与真实 ROUTER mock 测试，并通过 debug 构建
- [x] 5.8 覆盖地图视图缩放锚点、正逆变换、倍率边界和还原，随后通过 Android 核心测试、debug 构建和 G20 工程 Mock 截图检查导航地图占比、上下文动作、对话框与主页任务入口和姿态按钮布局
- [x] 5.9 覆盖深色主题关键颜色对比、语义令牌和排版层级，通过 Android 核心测试、debug 构建及 G20 浅色系统下的工程 Mock 截图检查主页、设置、建图和导航对话框
- [x] 5.10 通过 Android 核心测试、debug 构建和 G20 工程 Mock 截图确认六种姿态动作同时可见且不存在更多动作入口，全程不下发运动命令
- [x] 5.11 通过 Android 构建和 G20 真实主控快照确认多项阻断原因可滚动查看，验证过程不下发地图或运动命令
- [x] 5.12 覆盖待机选图、其他动作不可用但当前加载允许、当前步骤真实阻断和详情滚动复位测试；安装 G20 后用真实主控快照确认页面不再误报阻断，全程不下发地图或运动命令
- [x] 5.13 覆盖等待初始位姿的状态文案和权威当前地图自动恢复预览测试；覆盖安装 G20 后确认预览无需手动重试即可恢复，全程不再下发地图或运动命令
- [x] 5.14 通过 Android debug 构建，并在 G20 非全屏定位导航页放大和平移地图，截图确认地图图像和叠加层不越过地图组件边界
- [x] 5.15 删除人工接管协议、控制器接口和导航 UI，只保留取消导航；覆盖取消后地图与定位保留、旧路线清理和再次规划的 reducer 与 Presenter 测试
- [ ] 5.16 与 NX 主控联调取消确认超时和迟到结果，确认旧任务不会让导航页永久停在执行中，并通过 Android 核心测试与 debug 构建
